# secrets / local config (never commit)
.env
.env.*
!.env.example
secrets.yaml
!secrets.yaml.example

# python
__pycache__/
*.pyc
.venv/
venv/

# esphome build artifacts
.esphome/

# device flash backups (large binaries, keep local only)
firmware-backups/

# persisted admin config (contains real hosts/tokens/password hash)
data/

# macOS
.DS_Store
