# Copy to secrets.yaml and fill in. secrets.yaml is gitignored.
# The firmware lists both networks; each board connects to whichever it finds.
wifi_jb_ssid: "your-work-ssid"
wifi_jb_password: "your-work-password"
wifi_home_ssid: "your-home-ssid"
wifi_home_password: "your-home-password"
